# Workstation-side reconstruction and visualization dependencies.
# Install with:
#   python -m pip install -r requirements-workstation.txt

imageio-ffmpeg==0.6.0
opencv-python-headless==4.13.0.92
PyYAML==6.0.3
